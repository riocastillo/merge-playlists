<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Domine:wght@700&family=Ubuntu+Mono&display=swap"
        rel="stylesheet">
</head>

<body>
    <div class="<% if (selected) { %> hide <% } else { %> firstButton <% } %>" class="menu firstButton">
        <button class="dropbtn openLists <% if (!selected) { %> biggerFont<% }%>" onclick="openList(event)">
            <%= playlist[0].displayName %>'s playlists
        </button>

    </div>
    <div id="listModal" class="modal">
        <!-- Modal content -->
        <div class="modal-content edit">
            <span class="exit">&times;</span>
            <h2 class="copied key">Hold down the Ctrl key (for Windows) or Command (for Macs) to select two playlists
            </h2>
            <form id="form2">
                <select multiple class="choose" id="names">
                    <% for(let i=0; i < playlist.length; i++) { %>
                        <option value="<%= playlist[i].id %>">
                            <%= playlist[i].name %>
                        </option>
                        <% } %>
                </select>

            </form>
            <input type="text" id="newPlaylistName" placeholder="enter new playlist name"></input>
            <button class="goToPlaylist" id="submit">submit</button>

        </div>
    </div>
    <% if(selected) { %>
        <div class="menu">
            <button class="dropbtn openLists" onclick="openList(event)">
                <%= playlist[0].displayName %>'s playlists
            </button>
        </div>
        <section class="onePlay">
            <section class="nav">
                <section class="borderLeft">
                    <p class="navLeft">playlist one</p>
                </section>
                <section class="borderRight">
                    <p class="navRight">
                        <%= playlist[0].displayName %>
                    </p>
                </section>
            </section>
            <p class="playlistTitle">Title: <%= selected[0].name%>
            </p>
        </section>
        <section class="second onePlay">
            <section class="nav">
                <section class="borderLeft">
                    <p class="navLeft">playlist two</p>
                </section>
                <section class="borderRight">
                    <p class="navRight">
                        <%= playlist[0].displayName %>
                    </p>
                </section>
            </section>
            <p class="playlistTitle">Title: <%= selected[1].name%>
            </p>
        </section>

        <section class="buttons">
            <div class="merge buttonFlip" onclick="openWindow(event)" data-url="<%= playlistURL %>">
                <div class="mergeBtn">Merge playlists</div>
                <div>Merging...</div>
            </div>
        </section>
        <% } %>
            <div id="myModal" class="modal">
                <!-- Modal content -->
                <div class="modal-content">
                    <span class="close">&times;</span>
                    <h2 class="copied">URL to your new playlist is copied to your clipboard!</h2>
                    <button class="goToPlaylist"  onclick="window.location.href='<%= playlistURL %>'">go to playlist here</button>
                </div>
            </div>
</body>
<script src="main.js"></script>

</html>